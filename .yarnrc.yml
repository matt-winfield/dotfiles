# Yarn Berry (v2+) config

# Exact versions only (no ^ or ~)
preferFixed: true

# Do not execute package lifecycle scripts on install
enableScripts: false

# Refuse packages published less than 48 hours ago
npmMinimalAgeGate: 2d
